import static io.println(int);
class IfElse {
	public static void main(String[] args) {
		IfElse nt;
		int[] arr;
		nt = new IfElse();
		arr = nt.ret_array();

		io.println(arr[1]);

		if(false) {

		} else if(true) {
		    if(false) {

		    } else {
		        if (8 < 3) {
                    io.println(0);
                } else if (8 < 5) {
                    io.println(1);
                } else if(9 < 7) {
                    io.println(2);
                } else {
                    io.println(3);
                }

                if(7 < 8) {
                    io.println(4);
                }

                io.println(5);

                if(9 < 8) {
                    io.println(6);
                }

                io.println(7);

                if(9 < 8) {
                    io.println(8);
                } else {
                    io.println(9);
                }

                if(7 < 8) {
                    io.println(8);
                } else {
                    io.println(9);
                }
		    }
		}
	}

	public int[] ret_array() {
	    int[] ret;
	    ret = new int[2];
	    ret[0] = 500;
	    ret[1] = 1000;
	    return ret;
	}

}